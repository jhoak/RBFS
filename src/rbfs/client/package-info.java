/**
This package contains the main code that powers the client as well as multiple
utility classes for easier manipulation of data. It contains everything from
the GUI components to the main class ("Client") that manages the windows and
handles interactions with a remote RBFS server.
@author James Hoak
*/
package rbfs.client;